{
  "name": "travis-status",
  "version": "1.0.0",
  "description": "A Travis CI status reporter",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager ./ \"Travis CI Status\" --asar --overwrite --no-package-manager --out dist"
  },
  "devDependencies": {
    "electron": "^1.8.2",
    "electron-packager": "^11.0.1",
    "svg2png": "^4.1.1"
  },
  "dependencies": {
    "moment": "^2.20.1",
    "request": "^2.83.0"
  }
}
